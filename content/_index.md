+++
# /search/ was a real page under Clarity (content/search.md, type = "search").
# Blowfish's search is a header overlay, not a route, so that file is gone --
# this alias keeps the old URL alive as a redirect to the home page rather
# than letting it 404. Press "/" or click the magnifier to search.
#
# The path must be absolute: on the home page a bare "search" resolves
# relative to "/" and Hugo rejects it as traversing outside the site root.
aliases = ["/search/"]
+++
